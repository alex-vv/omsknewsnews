services:
  postgres:
    image: 'postgres:16-alpine'
    environment:
      - POSTGRES_USER=omsknews
      - POSTGRES_DB=omsknews
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - '5432:5432'
    env_file:
      - .env.local  